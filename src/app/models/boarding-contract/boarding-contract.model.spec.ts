import { BoardingContract } from './boarding-contract.model';

describe('BoardingContract', () => {
  it('should create an instance', () => {
    expect(new BoardingContract()).toBeTruthy();
  });
});
